import React from 'react';

const Toolbar = () => {
    return (
        <div>
            <button>Refresh</button>
            <button>Add Customer</button>
            <button>Upload customer(csv)</button>
        </div>
    );
};

export default Toolbar;